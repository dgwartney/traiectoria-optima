"""Runnable demonstrations of the flight_planner library.

Each module is executable from the `flight_planner` directory as
`python -m demos.<name>`. They are illustrative scripts, not part of the
library's public API.
"""
