## Questions {#questions}

<div class="cards two">

<div class="card">

<span class="pill">Repository</span>

### `dgwartney/traiectoria-optima`

`make report` builds the report. `make deck` builds these slides **from the
report's own chapters**, so the two cannot disagree.

</div>

<div class="card">

<span class="pill mute">Reproducing a number</span>

### Every figure names a snapshot

`Snapshot.open()` re-hashes each file against its manifest before a row is
read, and refuses to proceed on a mismatch.

</div>

</div>
